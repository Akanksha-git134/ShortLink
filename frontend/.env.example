# Base URL of the deployed/local backend API
VITE_API_BASE_URL=http://localhost:5000

# Base used to build the clickable short link shown to users
# (same value as VITE_API_BASE_URL until you set up a custom domain)
VITE_SHORT_URL_BASE=http://localhost:5000
